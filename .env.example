# Database Configuration
# For local development: leave empty or use SQLite (default)
# For production (Vercel): use PostgreSQL connection string
DATABASE_URL=

# Session secret - generate a strong random key in production
# Required for session security - MUST change from default
SESSION_SECRET=your-super-secret-key-change-this-in-production

# PayPal Configuration (optional - for premium subscriptions)
# Only needed if implementing PayPal payment processing
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=

# Replit specific (optional)
REPLIT_DEV_DOMAIN=http://localhost:5000

# Vercel deployment environment (auto-set by Vercel)
# VERCEL=1
